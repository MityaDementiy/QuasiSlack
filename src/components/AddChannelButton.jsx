import React from 'react';

const AddChannelButton = () => {
  const vdom = (
      <>
        <hr/>
        <button className='btn btn-light btn-sm'>Add channel</button>
      </>
  );
  return vdom;
};

export default AddChannelButton;
